function extractImageLinks(): void {
  const htmlInput = (
    document.getElementById("htmlInput") instanceof HTMLTextAreaElement
  ).valueOf;
  const imageLinksContainer =
    document.getElementById("imageLinks") instanceof HTMLDivElement;
  const imageGridContainer =
    document.getElementById("imageGrid") instanceof HTMLDivElement;
}
